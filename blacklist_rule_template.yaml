# The blacklist rule will check a certain field against a blacklist, and match if it is in the blacklist.

es_host: elasticsearch
es_port: 9200

# (Required)
# Index to search, wildcard supported
index: "index-placeholder"

# (Required)
# Rule name, must be unique
name: name-placeholder

# (Required)
# Type of alert.
# the frequency rule type alerts when num_events events occur with timeframe time
type: blacklist

# (Required, blacklist)
# The name of the field to use to compare to the blacklist. If the field is null, those events will be ignored.
compare_key: compare-key-placeholder

# (Required, blacklist)
# A list of blacklisted values, and/or a list of paths to flat files which contain the blacklisted values
blacklist:
    - "!file blacklist-file-placeholder"

# (Required)
# A list of Elasticsearch filters used for find events
# These filters are joined with AND and nested in a filtered query
# For more info: http://www.elasticsearch.org/guide/en/elasticsearch/reference/current/query-dsl.html
filter:
- filter-type-placeholder:
    field-type-placeholder: "field-value-placeholder"
# (Required)
# The alert is use when a match is found
alert:
- "alert-placeholder"

# (required, email specific)
# a list of email addresses to send alerts to
#alert-option-placeholder:
#- "alert-option-value-placeholder"
